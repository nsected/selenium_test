console.log('testik');
console.log(process.argv);
// import testCaseExecutor from './testCaseExecutor';

module.exports =  function testSuitInit() {

    // testSuit = global.env.libse.testSuit;
    // config = global.env.libse.config;
    //
    //     console.log(2);
    //     describe(
    //         testSuit.name,
    //         () => {
    //             testSuit.cases.forEach((testCase) => {
    //                 it(
    //                     testCase.name,
    //                     done => {
    //                         testCaseExecutor(testCase, config, done);
    //                     }
    //                 )
    //             })
    //         }
    //     );
}